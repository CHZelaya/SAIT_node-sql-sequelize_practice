<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Courses</title>
</head>

<body>
    <h1>List of All Courses</h1>
    <ul>
        <% courses.forEach(course=> { %>
            <li>
                <%= course.coursename +" | "+course.description +" | "+course.credit%>
                <a href=" /edit-course/<%=course.id %>">Update</a>
                    <a href="/delete-course/<%= course.id %>">Delete</a>
            </li>
            <% }) %>
    </ul>
    <hr>
    <a href="/add-course">Add New Course</a>

</body>

</html>